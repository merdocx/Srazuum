# Core dependencies
aiogram==3.1.1
pyrogram==2.0.106
tgcrypto==1.2.5

# Database
sqlalchemy[asyncio]==2.0.23
asyncpg==0.29.0
alembic==1.12.1

# HTTP Client
httpx==0.25.2
# aiohttp устанавливается как зависимость aiogram, не нужно указывать отдельно

# Redis
redis==5.0.1
aioredis==2.0.1

# Environment
python-dotenv==1.0.0

# Logging
structlog==23.2.0

# Task Queue (optional)
# celery==5.3.4  # Не используется, закомментировано

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
pytest-cov==4.1.0
aiosqlite==0.19.0  # для тестовой БД

# Utilities
# pydantic устанавливается как зависимость aiogram
# pydantic-settings не нужен, используем BaseSettings из pydantic

# System monitoring
psutil==5.9.6

