# Техническое задание: Система платных подписок

**Версия:** 1.0  
**Дата:** 2025-01-04  
**Проект:** Srazuum  
**Статус:** Черновик

---

## 1. Общее описание

### 1.1. Назначение
Внедрение системы платных подписок для сервиса кросспостинга из Telegram в MAX с использованием платежной системы YooKassa.

### 1.2. Цели
- Монетизация сервиса через платные подписки
- Автоматизация оплаты и управления подписками
- Обеспечение бесплатного пробного периода для новых пользователей
- Автоматические уведомления о необходимости продления

### 1.3. Основные требования
- Тариф: 200 руб/месяц за 1 связь (CrosspostingLink)
- Платежная система: YooKassa
- Бесплатный период: 1 месяц для первой связи пользователя
- Автоматические уведомления о продлении
- Деактивация связи при непродлении
- Индивидуальная оплата для каждой связи

---

## 2. Функциональные требования

### 2.1. Тарифы и ценообразование

#### 2.1.1. Тарифный план
- **Один тариф:** 200 рублей в месяц за 1 связь (CrosspostingLink)
- **Бесплатный период:** 1 месяц для первой связи пользователя
- **Период оплаты:** 1 месяц (30 дней)

#### 2.1.2. Правила бесплатного периода
- Бесплатный период предоставляется **только для первой связи** пользователя
- Бесплатный период начинается с момента создания первой связи
- Все последующие связи требуют немедленной оплаты для активации
- Бесплатный период составляет 30 дней

#### 2.1.3. VIP пользователи
- **VIP пользователи** получают **бесплатный доступ ко всем связям без ограничений**
- Для VIP пользователей не требуется оплата подписок
- Все связи VIP пользователей активны постоянно
- Управление VIP статусом осуществляется через админ-панель
- VIP статус назначается администратором вручную

### 2.2. Модель данных

#### 2.2.1. Изменения в модели User

Добавить поле:
```python
is_vip: bool  # VIP статус пользователя (бесплатный доступ ко всем связям)
```

#### 2.2.2. Изменения в модели CrosspostingLink

Добавить следующие поля:
```python
# Поля подписки
subscription_status: str  # Enum: 'free_trial', 'active', 'expired', 'cancelled'
free_trial_end_date: Optional[datetime]  # Дата окончания бесплатного периода
subscription_end_date: Optional[datetime]  # Дата окончания оплаченного периода
is_first_link: bool  # Является ли это первой связью пользователя
last_payment_date: Optional[datetime]  # Дата последней оплаты
payment_id: Optional[str]  # ID платежа в YooKassa
payment_status: Optional[str]  # Статус платежа: 'pending', 'succeeded', 'canceled'
yookassa_payment_id: Optional[str]  # ID платежа от YooKassa
```

#### 2.2.4. Новая модель PaymentHistory (опционально, для истории платежей)
```python
class PaymentHistory(Base):
    id: int
    link_id: int  # FK to CrosspostingLink
    user_id: int  # FK to User
    amount: Decimal  # Сумма платежа
    currency: str  # RUB
    payment_date: datetime
    yookassa_payment_id: str
    status: str  # 'succeeded', 'canceled', 'refunded'
    period_start: datetime  # Начало оплаченного периода
    period_end: datetime  # Конец оплаченного периода
```

### 2.3. Логика работы подписок

#### 2.3.1. Создание связи

**Первая связь пользователя:**
1. При создании первой связи (`is_first_link=True`)
2. Устанавливается `subscription_status='free_trial'`
3. Устанавливается `free_trial_end_date = now() + 30 days`
4. Связь активируется сразу (`is_active=True`)
5. Запускается бесплатный период на 30 дней

**Последующие связи:**
1. При создании второй и последующих связей (`is_first_link=False`)
2. Связь создается, но `is_active=False`
3. После успешного создания связи бот автоматически запрашивает оплату
4. Связь остается неактивной до оплаты
5. После успешной оплаты связь активируется (`is_active=True`, `subscription_status='active'`, `subscription_end_date = now() + 30 days`)
6. Если пользователь не оплатил сразу, оплату можно произвести позже через команду `/pay_link [link_id]`

#### 2.3.2. Продление подписки

**VIP пользователи:**
- Продление не требуется, подписка бессрочная
- Все связи остаются активными без ограничений

**Обычные пользователи:**
1. При успешной оплате `subscription_end_date` **прибавляется** к текущей дате окончания
2. Если `subscription_end_date` уже прошла, то новая дата = `now() + 30 days`
3. Если `subscription_end_date` еще не прошла, то новая дата = `subscription_end_date + 30 days`
4. После продления `subscription_status = 'active'`
5. Если связь была деактивирована, она автоматически активируется

**Пример:**
- Текущая дата окончания: 2025-02-15
- Оплата произведена: 2025-02-10
- Новая дата окончания: 2025-03-15 (прибавлено 30 дней к 2025-02-15)

#### 2.3.3. Деактивация при непродлении

**VIP пользователи:**
- Деактивация не применяется, связи остаются активными

**Обычные пользователи:**
1. При наступлении даты окончания (`subscription_end_date` или `free_trial_end_date`)
2. Если подписка не продлена, связь деактивируется (`is_active=False`)
3. `subscription_status` меняется на `'expired'`
4. Кросспостинг для этой связи прекращается
5. Активация возможна только после оплаты

#### 2.3.4. Удаление связи

1. При удалении связи оплаченный срок **обнуляется**
2. Все данные о подписке удаляются вместе со связью
3. Возврат средств не предусмотрен

### 2.4. Уведомления о продлении

#### 2.4.1. Временные метки уведомлений
Уведомления отправляются за:
- **7 дней** до окончания срока
- **3 дня** до окончания срока
- **1 день** до окончания срока
- **1 час** до окончания срока

#### 2.4.2. Логика уведомлений
1. Уведомления отправляются только для активных связей (`is_active=True`)
2. Уведомления отправляются через Telegram бот (личные сообщения пользователю)
3. Каждое уведомление отправляется только один раз
4. Уведомления проверяются периодически (например, каждые 5 минут через cron или фоновую задачу)

#### 2.4.3. Содержание уведомления
```
📢 Напоминание о продлении подписки

Связь: [Название связи]
Истекает через: [X дней/часов]

Продлите подписку сейчас, чтобы не прерывать кросспостинг.

💳 Продлить подписку - /pay_link [link_id]
📋 Мои связи - /list_channels
```

### 2.5. Интеграция с YooKassa

#### 2.5.1. Регистрация платежа

1. Пользователь инициирует оплату (через бот или админ-панель)
2. Создается платеж в YooKassa API
3. Сохраняется `yookassa_payment_id` и `payment_status='pending'`
4. Пользователю отправляется ссылка на оплату
5. После успешной оплаты YooKassa отправляет webhook

#### 2.5.2. Обработка webhook от YooKassa

1. YooKassa отправляет уведомление о статусе платежа
2. Проверяется подпись уведомления (безопасность)
3. Обновляется статус платежа в БД
4. При успешной оплате (`status='succeeded'`):
   - Продлевается подписка (логика из 2.3.2)
   - Активируется связь (`is_active=True`)
   - Отправляется уведомление пользователю об успешной оплате

#### 2.5.3. Возвраты и отмены

- Возвраты не предусмотрены при удалении связи
- Отмена платежа обрабатывается автоматически (связь остается неактивной)

---

## 3. Пользовательский интерфейс (UI/UX)

### 3.1. Telegram Bot

#### 3.1.1. Команды и кнопки

**Команды:**
- `/pay_link [link_id]` - оплатить/продлить конкретную связь
- `/my_subscriptions` - список всех связей с информацией о подписках
- `/subscription_info [link_id]` - детальная информация о подписке

**Кнопки в списке связей:**
- `🔄 Продлить` - для активных подписок (отображается всегда, если есть хотя бы одна активная подписка)
- `⏰ Истекает через X дней` - индикатор срока подписки
- `⭐ VIP` - индикатор VIP статуса пользователя (опционально)

**Примечание:** 
- Кнопка "Оплатить" не отображается. Для неоплаченных/истекших связей оплата запрашивается автоматически после создания связи (если это не первая связь) или через команду `/pay_link [link_id]`
- При нажатии на "Продлить" пользователю предлагается выбрать связь для продления из списка активных подписок
- Для VIP пользователей кнопка "Продлить" не отображается, все связи показаны как активные

#### 3.1.2. Информация о подписке в деталях связи

При просмотре деталей связи (`/list_channels` → выбор связи):

**Для обычных пользователей:**
```
📊 Связь #123

Telegram: @channel1
MAX: @channel2

📅 Подписка:
Статус: ✅ Активна
Тип: Бесплатный период / Оплаченная подписка
Осталось: 15 дней
Окончание: 2025-01-20 12:00

💳 Действия:
[Продлить подписку] [Удалить связь]
```

**Для VIP пользователей:**
```
📊 Связь #123 ⭐ VIP

Telegram: @channel1
MAX: @channel2

📅 Подписка:
Статус: ✅ Активна (VIP)
Тип: Бесплатная подписка

💳 Действия:
[Удалить связь]
```

#### 3.1.3. Процесс оплаты при создании связи

1. Пользователь создает связь (если это не первая связь пользователя)
2. После успешного создания связи бот автоматически запрашивает оплату:
   ```
   📊 Связь создана
   
   Telegram: @channel1
   MAX: @channel2
   
   ⚠️ Для активации связи требуется оплата подписки.
   
   Сумма: 200 ₽
   Период: 30 дней
   
   После оплаты связь будет активирована на 30 дней.
   
   [💳 Оплатить подписку]
   ```
3. Пользователь нажимает "Оплатить подписку"
4. Бот отправляет кнопку с ссылкой на оплату в YooKassa
5. Пользователь переходит по ссылке и оплачивает
6. После оплаты бот автоматически получает уведомление и активирует связь
7. Пользователь получает подтверждение:
   ```
   ✅ Оплата успешна!
   
   Связь активирована до: 2025-02-20 12:00
   Кросспостинг запущен.
   ```

**Примечание:** Если пользователь не оплатил сразу, связь остается неактивной. Оплату можно произвести позже через команду `/pay_link [link_id]`.

#### 3.1.4. Процесс продления подписки

1. Пользователь нажимает кнопку "Продлить" (в списке связей или в деталях связи)
2. Если у пользователя несколько активных подписок, бот показывает список:
   ```
   🔄 Продление подписки
   
   Выберите связь для продления:
   
   1. Telegram @channel1 → MAX @channel2
      Осталось: 5 дней
   
   2. Telegram @channel3 → MAX @channel4
      Осталось: 10 дней
   
   [Отмена]
   ```
3. Пользователь выбирает связь из списка
4. Бот показывает информацию о продлении:
   ```
   💳 Продление подписки
   
   Связь: Telegram @channel1 → MAX @channel2
   Текущее окончание: 2025-01-20
   Сумма: 200 ₽
   Период: 30 дней
   Новое окончание: 2025-02-20
   
   [💳 Продлить подписку]
   ```
5. Пользователь нажимает "Продлить подписку"
6. Бот отправляет кнопку с ссылкой на оплату в YooKassa
7. Пользователь переходит по ссылке и оплачивает
8. После оплаты подписка продлевается (добавляется 30 дней к текущей дате окончания)
9. Пользователь получает подтверждение:
   ```
   ✅ Подписка продлена!
   
   Новая дата окончания: 2025-02-20 12:00
   ```

### 3.2. Админ-панель (Web)

#### 3.2.1. Страница связей (Links)

**Таблица связей:**
- Добавить колонку "Подписка"
- Отображать статус подписки (активна/истекла/бесплатный период)
- Отображать дату окончания подписки
- Отображать количество дней до окончания

**Фильтры:**
- Все связи
- Активные подписки
- Истекшие подписки
- Бесплатный период

#### 3.2.2. Детальная информация о связи

Добавить секцию "Подписка":
```
📅 Подписка
Статус: Активна / Истекла / Бесплатный период
Тип: Первая связь (бесплатно) / Платная подписка
Дата начала: 2025-01-01
Дата окончания: 2025-02-01
Осталось: 15 дней
Последний платеж: 2025-01-01 (200 ₽)
```

#### 3.2.3. Статистика (Dashboard)

Добавить метрики:
- Всего активных подписок
- Всего истекших подписок
- Количество VIP пользователей
- Сумма платежей за период (месяц/год)
- Количество новых подписок за период

#### 3.2.4. Управление VIP пользователями

**Страница пользователей:**
- Добавить колонку "VIP" с индикатором статуса
- Кнопка/переключатель для установки/снятия VIP статуса
- Фильтр "VIP пользователи"

**Детальная информация о пользователе:**
- Поле "VIP статус" с переключателем
- Информация о дате назначения VIP (опционально)
- Информация о том, кто назначил VIP (опционально)

**Логика изменения VIP статуса:**
1. Администратор включает/выключает VIP статус через админ-панель
2. При включении VIP:
   - Все существующие связи пользователя автоматически становятся активными
   - Для всех связей устанавливается `subscription_status='vip'`
   - Поля `free_trial_end_date` и `subscription_end_date` обнуляются (`NULL`), так как для VIP пользователей они не используются
   - Останавливаются уведомления о продлении
3. При выключении VIP:
   - Для всех связей пересчитывается статус подписки:
     - Если есть активная подписка, статус остается 'active'
     - Если подписка истекла, статус меняется на 'expired'
     - Первая связь переходит в 'free_trial' (если период не истек) или 'expired'
   - Связи деактивируются, если нет активной подписки

---

## 4. Техническая реализация

### 4.1. База данных

#### 4.1.1. Миграция для User

```python
# Добавить поле:
- is_vip (BOOLEAN, default=False, nullable=False)
```

#### 4.1.2. Миграция для CrosspostingLink

```python
# Добавить поля:
- subscription_status (VARCHAR, default='free_trial')
- free_trial_end_date (TIMESTAMP, nullable)
- subscription_end_date (TIMESTAMP, nullable)
- is_first_link (BOOLEAN, default=False)
- last_payment_date (TIMESTAMP, nullable)
- payment_id (VARCHAR, nullable)
- payment_status (VARCHAR, nullable)
- yookassa_payment_id (VARCHAR, nullable)
```

**Индексы:**
- Добавить индекс на `user.is_vip` для быстрой фильтрации

#### 4.1.2. Новая таблица PaymentHistory (опционально)

```sql
CREATE TABLE payment_history (
    id SERIAL PRIMARY KEY,
    link_id INTEGER REFERENCES crossposting_links(id) ON DELETE CASCADE,
    user_id INTEGER REFERENCES users(id),
    amount DECIMAL(10, 2) NOT NULL,
    currency VARCHAR(3) DEFAULT 'RUB',
    payment_date TIMESTAMP NOT NULL,
    yookassa_payment_id VARCHAR(255) UNIQUE NOT NULL,
    status VARCHAR(50) NOT NULL,
    period_start TIMESTAMP NOT NULL,
    period_end TIMESTAMP NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);
```

### 4.2. Интеграция с YooKassa

#### 4.2.1. Установка библиотеки

```python
# requirements.txt
yookassa==3.0.0  # или актуальная версия
```

#### 4.2.2. Конфигурация

```python
# config/settings.py
YOOKASSA_SHOP_ID: str
YOOKASSA_SECRET_KEY: str
YOOKASSA_WEBHOOK_URL: str  # URL для получения webhook
YOOKASSA_RETURN_URL: str  # URL возврата после оплаты
```

#### 4.2.3. Модуль для работы с платежами

Создать `app/payments/yookassa_client.py`:
- Метод создания платежа
- Метод проверки статуса платежа
- Метод обработки webhook
- Метод проверки подписи webhook

### 4.3. Фоновые задачи

#### 4.3.1. Проверка истекших подписок

Периодическая задача (каждые 5-10 минут):
- Найти связи, у которых `subscription_end_date < now()` или `free_trial_end_date < now()`
- Деактивировать связи (`is_active=False`, `subscription_status='expired'`)
- Отправить уведомление пользователю об истечении подписки

#### 4.3.2. Отправка уведомлений о продлении

Периодическая задача (каждые 5 минут):
- Найти связи обычных пользователей (где `user.is_vip=False`), для которых нужно отправить уведомление:
  - `subscription_end_date` или `free_trial_end_date` через 7 дней, 3 дня, 1 день, 1 час
- Исключить связи с `subscription_status='vip'`
- Проверить, не отправлялось ли уже уведомление для этого периода
- Отправить уведомление пользователю

**Способы реализации:**
1. Отдельная таблица `subscription_notifications` для отслеживания отправленных уведомлений
2. Или использовать поле `last_notification_date` в CrosspostingLink

#### 4.3.3. Реализация фоновых задач

Варианты:
1. **Celery** (рекомендуется) - асинхронные задачи
2. **APScheduler** - планировщик задач
3. **Cron + скрипты** - простой вариант для начала

### 4.4. API эндпоинты

#### 4.4.1. Webhook от YooKassa

```
POST /api/payments/webhook
```

Обработка уведомлений от YooKassa:
- Проверка подписи
- Обновление статуса платежа
- Продление подписки при успешной оплате

#### 4.4.2. Создание платежа

```
POST /api/payments/create
Body: { link_id: int }
```

Создание платежа в YooKassa и возврат ссылки на оплату.

---

## 5. Безопасность

### 5.1. Обработка платежей

1. **Проверка подписи webhook** - все уведомления от YooKassa должны проверяться на подлинность
2. **HTTPS обязателен** - все запросы к платежным API только по HTTPS
3. **Логирование платежей** - все операции с платежами должны логироваться
4. **Валидация данных** - проверка всех входящих данных от YooKassa

### 5.2. Защита от мошенничества

1. **Проверка владельца связи** - только владелец связи может оплачивать свою связь
2. **Лимиты платежей** - проверка на дублирующиеся платежи
3. **Таймауты** - отмена платежей, которые не завершились за определенное время

---

## 6. Уведомления

### 6.1. Типы уведомлений

1. **Напоминание о продлении** (за 7 дней, 3 дня, 1 день, 1 час)
2. **Подписка истекла** - уведомление при деактивации
3. **Оплата успешна** - подтверждение успешной оплаты
4. **Оплата не прошла** - уведомление об ошибке оплаты

### 6.2. Формат уведомлений

Все уведомления отправляются через Telegram бот в личные сообщения пользователю.

---

## 7. Тестирование

### 7.1. Тестовые сценарии

1. **Создание первой связи** - проверка бесплатного периода
2. **Создание второй связи** - проверка требования оплаты
3. **Оплата подписки** - полный цикл оплаты через YooKassa
4. **Продление подписки** - проверка прибавления срока
5. **Истечение подписки** - проверка деактивации
6. **Удаление связи** - проверка обнуления подписки
7. **Уведомления** - проверка отправки в нужное время

### 7.2. Тестирование YooKassa

Использовать тестовый режим YooKassa для разработки и тестирования.

---

## 8. Миграция данных

### 8.1. Обработка существующих связей

Для существующих связей при миграции:
1. Определить, какая связь является первой для каждого пользователя
2. Установить `is_first_link=True` для первых связей
3. Установить `free_trial_end_date = created_at + 30 days` для первых связей
4. Установить `subscription_status='free_trial'` для первых связей
5. Для остальных связей установить `subscription_status='expired'` и `is_active=False`

**Вариант:** Предоставить бесплатный период для всех существующих связей на 30 дней с момента внедрения.

---

## 9. Этапы реализации

### Этап 1: Подготовка (1-2 дня)
1. Проектирование модели данных (включая VIP статус)
2. Создание миграций БД (User.is_vip, CrosspostingLink подписки)
3. Настройка YooKassa (тестовый режим)
4. Установка необходимых библиотек

### Этап 2: Базовый функционал (3-5 дней)
1. Изменение модели CrosspostingLink
2. Реализация логики бесплатного периода
3. Интеграция с YooKassa (создание платежей)
4. Обработка webhook от YooKassa
5. Базовый UI в боте (команды оплаты)

### Этап 3: Уведомления и автоматизация (2-3 дня)
1. Реализация фоновых задач для проверки подписок
2. Реализация системы уведомлений
3. Автоматическая деактивация истекших подписок

### Этап 4: UI/UX (2-3 дня)
1. Улучшение интерфейса бота
2. Добавление информации о подписках в админ-панель
3. Добавление управления VIP статусом в админ-панель
4. Статистика платежей в админ-панели
5. Отображение VIP статуса в интерфейсе бота (опционально)

### Этап 5: Тестирование и доработка (2-3 дня)
1. Тестирование всех сценариев
2. Исправление найденных ошибок
3. Оптимизация производительности

**Общий срок:** 10-16 дней

---

## 10. VIP пользователи

### 10.1. Описание

VIP пользователи получают бесплатный доступ ко всем связям без ограничений. Управление VIP статусом осуществляется администратором через админ-панель.

### 10.2. Логика работы

1. **Для VIP пользователей:**
   - Все связи создаются бесплатно и сразу активируются
   - Не требуется оплата подписок
   - Нет ограничений на количество связей
   - Не отправляются уведомления о продлении
   - Связи не деактивируются при истечении срока

2. **Управление VIP статусом:**
   - Только администратор может назначать/снимать VIP статус
   - Изменение статуса через админ-панель (страница пользователей)
   - При включении VIP: все существующие связи пользователя активируются
   - При выключении VIP: пересчет статусов подписок для всех связей

3. **Отображение в интерфейсе:**
   - В админ-панели: индикатор VIP статуса в списке пользователей
   - В боте: опционально отображать VIP статус (например, значок ⭐)

### 10.3. Технические детали

- Поле `is_vip` добавляется в модель `User`
- При проверке подписок всегда проверять `user.is_vip`
- Для VIP пользователей `subscription_status='vip'` для всех связей
- Поля `free_trial_end_date` и `subscription_end_date` обнуляются (`NULL`) при назначении VIP статуса
- Уведомления и проверки подписок пропускаются для VIP пользователей

---

## 11. Вопросы для уточнения

1. **Возврат средств:** Нужен ли возврат средств при удалении связи? (Сейчас: нет возврата)
2. **Грейс-период:** Нужен ли грейс-период после истечения подписки? (Сейчас: нет, деактивация сразу)
3. **Уведомления в админ-панели:** Нужны ли уведомления в админ-панели о истекающих подписках?
4. **Статистика платежей:** Нужна ли детальная статистика платежей для пользователей?
5. **Промокоды/скидки:** Планируются ли промокоды или скидки в будущем?
6. **VIP статус в боте:** Нужно ли отображать VIP статус пользователя в боте? (Опционально)
7. **История VIP:** Нужна ли история назначения/снятия VIP статуса? (Опционально)

---

## 12. Дополнительные рекомендации

1. **Мониторинг:** Настроить мониторинг успешности платежей и уведомлений
2. **Логирование:** Детальное логирование всех операций с платежами
3. **Резервное копирование:** Убедиться, что данные о подписках включены в бэкапы
4. **Документация:** Подготовить документацию для пользователей по оплате подписок

---

**Примечания:**
- Все суммы в рублях (RUB)
- Все даты и время в UTC (конвертация в локальное время для отображения)
- Период подписки: 30 дней (1 месяц)
- Минимальная сумма платежа в YooKassa: проверьте актуальные лимиты

